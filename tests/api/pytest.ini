[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --color=yes --alluredir=allure-results
markers =
    smoke: 冒烟用例，最小集合
    idempotent: 幂等场景，验证重复请求行为
    cross: 跨链路用例，验证多模块协作
